package auth

import (
	// "api_gateway/config"
	// handler "api_gateway/handlers"
	// "net/http"
	"testing"
)

func TestAuth(t *testing.T) {
	// expectedDestinations := []config.Destination{
	// 	{
	// 		URL:        "http://192.168.1.999:8086",
	// 		PathPrefix: "serv25",
	// 	},
	// }

	// mux := http.NewServeMux()
	// mux.Handle("/", Auth(http.HandlerFunc(handler.ProxyHandler(expectedDestinations))))

}
